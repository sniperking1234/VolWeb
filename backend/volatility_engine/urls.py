from django.urls import path
from .views import (
    EvidencePluginsView,
    PluginArtefactsView,
    TimelinerArtefactsView,
    TimelinerTask,
    HandlesTask,
    ProcessDumpPslistTask,
    ProcessDumpMapsTask,
    FileDumpTask,
    TasksApiView,
    EnrichedProcessView,
    PauseExtractionTask,
    ResumeExtractionTask,
    StopExtractionTask,
    RestartRuleCompilationTask,
    RestartRulesetCompilationTask,
    YaraScanTask,
    YaraScanHistoryView,
    YaraScanDetailView,
    AvailablePluginsView,
    SelectiveExtractionTask,
)

urlpatterns = [
    path(
        "evidence/<int:evidence_id>/plugins/",
        EvidencePluginsView.as_view(),
        name="evidence-plugins",
    ),
    path(
        "evidence/<int:evidence_id>/plugin/<str:plugin_name>/",
        PluginArtefactsView.as_view(),
        name="evidence-plugin-artefacts",
    ),
    path(
        "evidence/<int:evidence_id>/plugin/<str:plugin_name>/artefacts/",
        TimelinerArtefactsView.as_view(),
    ),
    path("evidence/tasks/timeliner/", TimelinerTask.as_view()),
    path("evidence/tasks/handles/", HandlesTask.as_view()),
    path("evidence/tasks/dump/process/pslist/", ProcessDumpPslistTask.as_view()),
    path("evidence/tasks/dump/process/maps/", ProcessDumpMapsTask.as_view()),
    path("evidence/tasks/dump/file/", FileDumpTask.as_view()),
    path("evidence/<int:evidence_id>/tasks/", TasksApiView.as_view()),
    path(
        "evidence/<int:evidence_id>/process/<int:pid>/enriched/",
        EnrichedProcessView.as_view(),
    ),
    path("evidence/tasks/pause/", PauseExtractionTask.as_view()),
    path("evidence/tasks/resume/", ResumeExtractionTask.as_view()),
    path("evidence/tasks/stop/", StopExtractionTask.as_view()),
    path("yararules/tasks/restart/", RestartRuleCompilationTask.as_view()),
    path("yararulesets/tasks/restart/", RestartRulesetCompilationTask.as_view()),
    path("evidence/<int:evidence_id>/yarascan/history/", YaraScanHistoryView.as_view(), name="yarascan-history"),
    path("evidence/<int:evidence_id>/yarascan/<str:plugin_name>/", YaraScanDetailView.as_view(), name="yarascan-detail"),
    path("evidence/tasks/yarascan/", YaraScanTask.as_view()),
    path("evidence/<int:evidence_id>/available-plugins/", AvailablePluginsView.as_view(), name="available-plugins"),
    path("evidence/tasks/selective-extraction/", SelectiveExtractionTask.as_view(), name="selective-extraction"),
]
